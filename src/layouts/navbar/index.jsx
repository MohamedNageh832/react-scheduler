import "./style.css";

const Navbar = () => {
  return (
    <header className="navbar">
      <span className="navbar__logo">
        React <span className="navbar__text">scheduler</span>
      </span>
    </header>
  );
};

export default Navbar;
